{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build" : "tsc -b",
    "start" : "node ./dist/index.js",
    "dev" : "bun run build && bun run start"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@repo/db" : "*",
    "@repo/typescript-config" : "*"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
